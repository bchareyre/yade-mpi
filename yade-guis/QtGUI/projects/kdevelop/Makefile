#############################################################################
# Makefile for building: QtGUI
# Generated by qmake (1.07a) (Qt 3.3.4) on: Wed Jul  6 12:04:04 2005
# Project:  QtGUI.pro
# Template: subdirs
# Command: $(QMAKE) -o Makefile QtGUI.pro
#############################################################################

MAKEFILE =	Makefile
QMAKE    =	qmake
DEL_FILE =    rm -f
CHK_DIR_EXISTS= test -d
MKDIR    = mkdir -p
INSTALL_FILE= 
INSTALL_DIR = 
SUBTARGETS =	 \
		sub-QtGUI

first: all

all: Makefile $(SUBTARGETS)

QtGUI/$(MAKEFILE): 
	@$(CHK_DIR_EXISTS) "QtGUI" || $(MKDIR) "QtGUI"
	cd QtGUI && $(QMAKE) QtGUI.pro -o $(MAKEFILE)
sub-QtGUI: QtGUI/$(MAKEFILE) FORCE
	cd QtGUI && $(MAKE) -f $(MAKEFILE)

Makefile: QtGUI.pro  /usr/lib/qt-3.3/mkspecs/default/qmake.conf 
	$(QMAKE) -o Makefile QtGUI.pro
qmake: qmake_all
	@$(QMAKE) -o Makefile QtGUI.pro

all: $(SUBTARGETS)
qmake_all: QtGUI/$(MAKEFILE)
	 ( [ -d QtGUI ] && cd QtGUI ; grep "^qmake_all:" $(MAKEFILE) && $(MAKE) -f $(MAKEFILE) qmake_all; ) || true
clean uicables mocables uiclean mocclean lexclean yaccclean : qmake_all FORCE
	 ( [ -d QtGUI ] && cd QtGUI ; $(MAKE) -f $(MAKEFILE) $@; ) || true
uninstall_subdirs: qmake_all FORCE
	 ( [ -d QtGUI ] && cd QtGUI ; $(MAKE) -f $(MAKEFILE) uninstall; ) || true
install_subdirs: qmake_all FORCE
	 ( [ -d QtGUI ] && cd QtGUI ; $(MAKE) -f $(MAKEFILE) install; ) || true
distclean: qmake_all FORCE
	 ( [ -d QtGUI ] && cd QtGUI ; $(MAKE) -f $(MAKEFILE) $@; $(DEL_FILE) $(MAKEFILE); ) || true

install:  install_subdirs

uninstall:  uninstall_subdirs

FORCE:

